{% embed 'partials/with_header.html.twig' %}

    {% block content %}
        {{ parent().header.banner_img }}
        <div class="blog-header">
            <h2>{{ page.title }}</h2>
        </div>
        <div class="wrapper blog">
            {{ page.content }}
        </div>
    {% endblock %}

{% endembed %}