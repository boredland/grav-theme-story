{% embed 'partials/base.html.twig' %}

    {% block content %}
        <div class="blog-header">
            <h2>{{ page.title }}</h2>
        </div>
        <div class="wrapper blog">
            {{ page.content }}
        </div>
    {% endblock %}

{% endembed %}